[package]
name = "lattice-protocol"
version = "0.1.0"
edition = "2021"
description = "Lattice's internal protocols"

[dependencies]
thiserror = "1.0.38"
miette = { version = "5.5.0", features = ["fancy"] }

